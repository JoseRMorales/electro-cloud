{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt && cd app && uvicorn API.main:app --reload --port 8001"

  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
