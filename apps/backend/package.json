{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt && uvicorn API.main:app --reload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
